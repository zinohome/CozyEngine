# CozyEngine 项目协作指南

> **文档版本**: v1.0  
> **更新日期**: 2026-01-09

本指南基于 `.cursorrules` 规则，为 CozyEngine 项目的所有贡献者（包括人类和 AI）提供统一的协作标准。

---

## 📋 目录

1. [语言与沟通](#1-语言与沟通)
2. [设计文档约束](#2-设计文档约束)
3. [文档规范](#3-文档规范)
4. [架构决策记录（ADR）](#4-架构决策记录adr)
5. [Python/FastAPI 最佳实践](#5-pythonfastapi-最佳实践)
6. [AI 应用工程实践](#6-ai-应用工程实践)
7. [测试与质量门槛](#7-测试与质量门槛)
8. [仓库卫生](#8-仓库卫生)

---

## 1. 语言与沟通

### 1.1 沟通语言
- **默认使用中文（简体）** 进行代码审查、PR 讨论、代码注释和文档。
- 所有面向用户或跨语言的内容须注明多语言支持计划。

### 1.2 决策流程
- **先对齐目标与约束**，再动手修改代码。
- **重要决策必须在设计文档中落地**，见 [架构决策记录（ADR）](#4-架构决策记录adr) 规则。
- 避免在 PR 中进行架构级讨论；应在 Issues 或设计文档中先行对齐。

---

## 2. 设计文档约束

### 2.1 设计文档为准
- ✅ **开发必须以** `docs/engine-v2/` **的设计文档为准**，涵盖：
  - 编排与上下文构建
  - 插件系统设计
  - 接口规范（OpenAI 兼容 + CozyChat 兼容）
  - 降级与容错策略
  - 可观测性与监控

### 2.2 代码与设计的冲突解决
- **若发现实现与设计冲突**：
  1. 优先权：**设计文档 > 现有代码**
  2. 行动：更新原设计文档（升级版本号）
  3. 同步：修改代码以符合最新设计
  4. 禁止：仅在代码中"默默变更"设计而不更新文档

### 2.3 设计文档位置
- **所有文档必须位于** `docs/` **目录下**，按用途放入子目录：
  - `docs/engine-v2/` - 引擎核心设计文档
  - `docs/standards/` - 技术标准与规范
  - `docs/adr/` - 架构决策记录
  - `docs/runbooks/` - 运维手册
  - `docs/plans/` - 开发计划书
  - `docs/drafts/` - 草稿与讨论稿

---

## 3. 文档规范

### 3.1 文件命名规则
**必须包含版本与日期**：`...-vX.Y-YYYY-MM-DD.md`

示例：
- ✅ `05-插件系统设计-v2.0-2026-01-09.md`
- ✅ `ADR-001-上下文缓存策略-v1.0-2026-01-09.md`
- ❌ `plugin-system.md`
- ❌ `ADR-001-上下文缓存策略.md`

### 3.2 文档头部必需字段
每个文档顶部必须包含：

```markdown
> **文档版本**: vX.Y  
> **更新日期**: YYYY-MM-DD
```

### 3.3 版本号升级规则
根据改动程度升级版本号：

| 改动类型 | 升级规则 | 示例 |
|---------|--------|------|
| **小改动** | Patch（可选） | v2.0 → v2.0.1 |
| **行为/接口/边界变化** | 至少 Minor | v2.0 → v2.1 |
| **架构级变化** | Major | v2.x → v3.0 |

### 3.4 禁止文档重复与分叉
- ❌ **禁止**：同一主题维护多个版本或分叉文档
- ✅ **正确方式**：
  - 同一文档中保留"变更历史"部分
  - 通过版本升级 + 日期标注体现演进
  - 删除旧版本文件（或存档至 `docs/archives/`）

---

## 4. 架构决策记录（ADR）

### 4.1 何时创建 ADR
创建 ADR 的决策**影响以下边界之一**：
- 🔌 **接口/API** - 公共接口的改变
- 📊 **数据** - 存储结构、数据流设计
- 🔐 **安全** - 权限、认证、加密策略
- 🎯 **可用性** - 服务级别、降级策略
- 🏗️ **架构** - 模块划分、依赖关系

### 4.2 ADR 格式
**路径**：`docs/adr/ADR-####-标题-v1.0-YYYY-MM-DD.md`

**必需部分**：
```markdown
> **文档版本**: v1.0  
> **更新日期**: YYYY-MM-DD  
> **决策日期**: YYYY-MM-DD

## 问题陈述
[简要描述面临的问题或需求]

## 决策
[采取的决策方案]

## 理由
[为什么选择这个方案？对比了哪些替代方案？]

## 影响范围
- [受影响的模块/功能]
- [涉及的接口变更]
- [迁移计划（若有）]

## 相关文档
- [链接到相关设计文档或 Issue]
```

### 4.3 规则
- ✅ **记录决策与理由**，不要复制大段设计文档内容
- ✅ **Link Back**：在原设计文档中引用相关 ADR
- ✅ **版本管理**：重大变更升级版本号

---

## 5. Python/FastAPI 最佳实践

### 5.1 类型注解（严格必需）
- ✅ 所有函数参数和返回值必须有类型注解
- ✅ 公共接口必须有清晰的 docstring（包含类型、含义、异常）
- ✅ 使用 `typing` 模块中的复杂类型：`Optional`, `Union`, `List`, `Dict` 等

示例：
```python
from typing import Optional, List
from fastapi import FastAPI, Depends
from pydantic import BaseModel

class MessageRequest(BaseModel):
    content: str
    personality_id: str

async def get_message_context(
    user_id: str,
    session_id: Optional[str] = None
) -> Dict[str, Any]:
    """获取消息上下文。
    
    Args:
        user_id: 用户 ID
        session_id: 会话 ID（可选）
    
    Returns:
        包含上下文的字典
    
    Raises:
        ValueError: 当 user_id 无效时
    """
    ...
```

### 5.2 异步优先
- ✅ **I/O 操作（HTTP/DB/Redis）使用 async**
- ✅ 避免在 event loop 中跑阻塞代码
- ✅ 使用 `asyncio.gather()` 并行多个异步操作
- ❌ 禁止 `time.sleep()`，使用 `asyncio.sleep()` 代替

### 5.3 依赖注入
- ✅ 使用 FastAPI `Depends()` 进行依赖注入
- ✅ API 层保持轻薄，不写业务规则
- ✅ 业务逻辑下沉到 Service 层

示例：
```python
async def verify_user(user_id: str) -> User:
    # 验证逻辑
    return user

@app.get("/messages")
async def list_messages(
    user: User = Depends(verify_user)
) -> List[Message]:
    """列表消息接口。"""
    ...
```

### 5.4 统一错误模型
- ✅ 所有异常必须映射到统一错误码结构
- 📖 参考：`docs/engine-v2/11-错误处理与可观测性-v2.0-2026-01-09.md`
- ✅ 错误响应包含：错误码、人类可读消息、追踪 ID

示例：
```python
class APIError(Exception):
    def __init__(
        self,
        code: str,
        message: str,
        status_code: int = 400,
        detail: Optional[Dict] = None
    ):
        self.code = code
        self.message = message
        self.status_code = status_code
        self.detail = detail

@app.exception_handler(APIError)
async def handle_api_error(request, exc: APIError):
    return JSONResponse(
        status_code=exc.status_code,
        content={
            "error_code": exc.code,
            "message": exc.message,
            "request_id": request.scope.get("request_id")
        }
    )
```

### 5.5 结构化日志
- ✅ **必须包含**：`request_id`, `user_id`, `session_id`, `personality_id`（若可得）
- ✅ 使用结构化日志库（如 `structlog` 或 `loguru`）
- ❌ **禁止记录**：密钥、密码、个人隐私信息（PII）
- ✅ 日志级别：DEBUG/INFO/WARNING/ERROR/CRITICAL

示例：
```python
import structlog

logger = structlog.get_logger()

@app.post("/chat")
async def chat(request: MessageRequest, request_id: str = Header(...)):
    logger.info(
        "chat_start",
        request_id=request_id,
        user_id=request.user_id,
        session_id=request.session_id
    )
    # ... 业务逻辑
    logger.info("chat_complete", request_id=request_id, duration_ms=duration)
```

---

## 6. AI 应用工程实践

### 6.1 上下文构建
- ✅ **支持并行获取**：Knowledge Base, User Profile, Chat Memory
- ✅ **超时控制**：每个上下文源有独立超时，防止长尾延迟
- ✅ **降级策略**：上下文源失败时的降级方案
- ✅ **Token 预算**：确保上下文总长不超过模型限制

参考：`docs/engine-v2/07-编排与上下文构建-v2.0-2026-01-09.md`

### 6.2 工具调用安全
- ✅ **白名单机制**：只允许预定义的工具集
- ✅ **权限校验**：工具执行前验证用户权限
- ✅ **审计日志**：记录所有工具调用及其结果
- ❌ **禁止任意执行**：不允许动态加载或执行任意代码

### 6.3 SSE 流式遵循 OpenAI 兼容格式
- ✅ 每条消息前缀：`data: `
- ✅ 流结束时发送：`data: [DONE]`
- ✅ 内容为 JSON，包含 `choices[0].delta.content`

示例：
```python
async def generate_stream(request: ChatRequest):
    async def stream_generator():
        async for chunk in llm_stream(request):
            yield f"data: {json.dumps({'choices': [{'delta': {'content': chunk}}]})}\n\n"
        yield "data: [DONE]\n\n"
    
    return StreamingResponse(stream_generator(), media_type="text/event-stream")
```

---

## 7. 测试与质量门槛

### 7.1 关键链路必须有测试
覆盖以下场景：
- 🔗 **上下文并行构建** - 测试多个上下文源同时失败的情况
- ⏱️ **超时降级** - 验证超时后的降级行为
- 🔄 **工具循环** - 测试工具调用的边界情况（最大循环数、权限拒绝等）
- 🔐 **权限边界** - 确保权限检查有效
- 📡 **SSE 协议** - 验证流的正确性和完整性

### 7.2 测试与文档同步
- ✅ 修改核心逻辑时**同步更新测试与文档版本**
- ✅ 新增接口时添加集成测试
- ✅ 破坏性变更时更新 ADR 和迁移指南

### 7.3 测试目录结构
```
src/
├── module/
│   ├── __init__.py
│   ├── core.py
│   └── ...
tests/
├── unit/
│   ├── test_module_core.py
│   └── ...
├── integration/
│   ├── test_chat_flow.py
│   └── ...
└── conftest.py
```

---

## 8. 仓库卫生

### 8.1 密钥与敏感信息
- ❌ **禁止**：将密钥、密码、API Token 写入代码/文档/日志
- ✅ **示例使用**：`.env.example` 或占位符（如 `YOUR_API_KEY_HERE`）
- ✅ **工具**: 使用 `git-secrets` 或类似工具防止意外提交

示例 `.env.example`:
```env
# AI 服务配置
LLM_API_KEY=YOUR_API_KEY_HERE
LLM_BASE_URL=https://api.openai.com/v1

# 数据库配置
DATABASE_URL=postgresql://user:password@localhost:5432/cozy_engine

# Redis 配置
REDIS_URL=redis://localhost:6379
```

### 8.2 避免大文件与生成物
- ❌ **禁止提交**：
  - 超过 10MB 的文件（除特殊情况）
  - 依赖包缓存（`__pycache__`, `.venv`, `node_modules` 等）
  - 生成物（编译输出、测试覆盖率报告等）
  - 模型权重或数据集（使用 Git LFS）

- ✅ **配置** `.gitignore`:
  ```
  # Python
  __pycache__/
  *.py[cod]
  *$py.class
  .venv/
  venv/
  
  # IDE
  .vscode/
  .idea/
  *.swp
  
  # Env
  .env
  .env.local
  
  # Generated
  *.log
  dist/
  build/
  *.egg-info/
  ```

### 8.3 提交卫生
- ✅ **有意义的提交信息**：用中文，清晰表达改动内容
- ✅ **原子性提交**：一个 commit 对应一个逻辑变更
- ✅ **PR 链接 Issue**：`Fixes #123` 或 `Related to #456`

示例提交信息：
```
feat: 实现上下文并行构建功能

- 支持 Knowledge/Profile/Memory 并行获取
- 添加超时控制和降级策略
- 补充单元测试

Fixes #42
```

---

## 📚 相关文档链接

- [docs/engine-v2/INDEX-v2.0-2026-01-09.md](../docs/engine-v2/INDEX-v2.0-2026-01-09.md) - 引擎设计文档总览
- [docs/engine-v2/11-错误处理与可观测性-v2.0-2026-01-09.md](../docs/engine-v2/11-错误处理与可观测性-v2.0-2026-01-09.md) - 错误处理规范
- [docs/standards/文档规范-v1.0-2026-01-09.md](../docs/standards/文档规范-v1.0-2026-01-09.md) - 文档编写标准

---

## 🔄 变更历史

| 版本 | 日期 | 变更内容 |
|-----|------|--------|
| v1.0 | 2026-01-09 | 初版：基于 `.cursorrules` 创建项目协作指南 |

---

**最后更新**: 2026-01-09  
**维护者**: CozyEngine Team
